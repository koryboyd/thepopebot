Long-term rules – inject into every context

• Every meaningful action should produce a git commit
• Never send secrets to the LLM
• Prefer local Ollama inference over cloud APIs
• Changes should be reversible (git revert / checkout)
• When researching: cite sources and commit findings